{
  "name": "nodal-sample-application",
  "version": "0.1.0",
  "description": "Nodal Sample Application",
  "main": "server.js",
  "engines": {
    "iojs": ">= 2.0.1"
  },
  "dependencies": {
    "nodal": "git://github.com/keithwhor/nodal.git#es6"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js --harmony_classes --use_strict"
  },
  "private": true
}
