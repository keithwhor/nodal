module.exports = (function() {

  "use strict";

  const Nodal = require('nodal');

  class {{= data.name }} extends Nodal.Model {

    preInitialize() {}
    postInitialize() {}

    /* Model Extensions */

  }

  {{= data.name }}.prototype.schema = Nodal.my.Schema.models.{{= data.name }};

  {{= data.name }}.prototype.externalInterface = [
{{~ data.columns :value:index}}    '{{= value }}'{{? data.rows.length - 1 !== index }},{{?}}{{~}}
  ];

  return {{= data.name }};

})();
